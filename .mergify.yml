queue_rules:
  - name: default
    queue_conditions: true
    merge_conditions: true

merge_protections:
  - name: VM7_GATE1_PRODUCT_DISABLE_CONTROL
    if:
      - base = main
    success_conditions: true

merge_protections_settings:
  auto_merge_conditions:
    - title = VM7_GATE1_MP_OFF_0917
